import type { Config, SyncRules, YandexMusicConfig } from './types.js'
import { loadConfig, isDebugMode } from './config.js'

export {
    type Config,
    type SyncRules,
    type YandexMusicConfig,
    loadConfig,
    isDebugMode,
}